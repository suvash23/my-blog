const { getPosts, getOnePost, createPost } = require("./post");
const { getComments, createComment } = require("./comment");

module.exports = {
  getPosts,
  getOnePost,
  createPost,
  getComments,
  createComment,
};
